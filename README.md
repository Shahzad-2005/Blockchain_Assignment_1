# Blockchain_Assignment_1
Single Zone IIoT Decentralized Identity Framework

Implements: batch registration → Merkle proof → temporary tokens → verification
→ resource authorization → revocation → security attacks → performance evaluation.

## Stack
- Python 3.14, Flask
- `cryptography` — ECDSA P-256
- `hashlib` — SHA-256
- `matplotlib` — graphs

## Install
```bash
pip install flask cryptography matplotlib requests